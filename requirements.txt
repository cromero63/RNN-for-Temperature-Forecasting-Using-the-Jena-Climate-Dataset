# Jena Climate RNN Activity — Python dependencies
# Install with:  pip install -r requirements.txt

# Core ML / data
torch>=2.2.0        # replaces tensorflow — CPU build; GPU: see pytorch.org
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
joblib>=1.3

# API backend (Step 5)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# Frontend (Step 6)
streamlit>=1.35
plotly>=5.20
requests>=2.31
